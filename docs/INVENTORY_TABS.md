# Навигация по вкладкам на складе

## Обзор

Страница склада теперь имеет систему вкладок для быстрой фильтрации продуктов по категориям.

## Функциональность

### 1. Автоматическое создание вкладок

```typescript
// Система автоматически создает вкладки на основе категорий продуктов
const categories = useMemo(() => {
  const categoryMap = new Map<string, { name: string; count: number; icon: string }>();
  
  items.forEach(item => {
    if (item.category) {
      const existing = categoryMap.get(item.category);
      if (existing) {
        existing.count++;
      } else {
        categoryMap.set(item.category, {
          name: item.category,
          count: 1,
          icon: getCategoryIcon(item.category)
        });
      }
    }
  });

  return Array.from(categoryMap.values()).sort((a, b) => 
    a.name.localeCompare(b.name, 'ru')
  );
}, [items]);
```

**Особенности:**
- Вкладки создаются динамически на основе продуктов на складе
- Каждая вкладка показывает количество продуктов в категории
- Категории сортируются по алфавиту (с поддержкой русского языка)
- Если категории нет в данных - вкладка не создается

### 2. Фильтрация продуктов

```typescript
const filteredItems = useMemo(() => {
  if (activeTab === 'all') {
    return items;
  }
  return items.filter(item => item.category === activeTab);
}, [items, activeTab]);
```

**Режимы:**
- `activeTab === 'all'` - показываются все продукты
- `activeTab === 'Фрукты'` - только продукты из категории "Фрукты"

### 3. Иконки категорий

```typescript
const getCategoryIcon = (category: string) => {
  const icons: Record<string, string> = {
    // English
    'Dairy': '🥛',
    'Vegetables': '🥕',
    'Meat': '🥩',
    'Fish': '🐟',
    'Fruits': '🍎',
    'Grains': '🌾',
    'Spices': '🧂',
    'Beverages': '🥤',
    // Русский
    'Молочные продукты и яйця': '🥛',
    'Овощи': '🥕',
    'Мясо': '🥩',
    'Рыба': '🐟',
    'Фрукты': '🍎',
    'Зерновые': '🌾',
    'Специи': '🧂',
    'Напитки': '🥤',
    'Другое': '📦',
  };
  return icons[category] || '📦';
};
```

**Как добавить новую категорию:**
1. Добавьте категорию в backend (таблица `categories`)
2. Добавьте иконку в `getCategoryIcon()` функцию (опционально)
3. Вкладка автоматически появится при добавлении первого продукта этой категории

## UI/UX особенности

### Активная вкладка
```tsx
<button
  className={
    activeTab === 'all'
      ? 'bg-indigo-600 text-white shadow-md'  // Активная
      : 'bg-gray-100 text-gray-700 hover:bg-gray-200'  // Неактивная
  }
>
  <span>📦</span>
  <span>Все</span>
  <span className="badge">{items.length}</span>
</button>
```

**Визуальные индикаторы:**
- Активная вкладка: синий фон (`indigo-600`), белый текст
- Неактивная вкладка: серый фон, темный текст
- Hover эффект: светло-серый фон
- Badge с количеством продуктов

### Адаптивность

```tsx
<div className="flex flex-wrap gap-2">
  {/* Вкладки переносятся на новую строку при недостатке места */}
</div>
```

**Поддержка устройств:**
- Desktop: вкладки в одну строку
- Tablet: переносится на 2-3 строки
- Mobile: каждая вкладка занимает всю ширину или переносится

### Empty State

Если в выбранной категории нет продуктов:

```tsx
{!loading && items.length > 0 && filteredItems.length === 0 && (
  <div className="empty-state">
    <Package />
    <h3>В категории "{activeTab}" нет продуктов</h3>
    <p>Выберите другую категорию или добавьте новый продукт</p>
  </div>
)}
```

## Производительность

### useMemo оптимизация

Обе функции используют `useMemo` для избежания лишних вычислений:

```typescript
// Пересчитывается только при изменении items
const categories = useMemo(() => { ... }, [items]);
const filteredItems = useMemo(() => { ... }, [items, activeTab]);
```

**Когда происходит пересчет:**
- `categories`: при добавлении/удалении продуктов
- `filteredItems`: при смене вкладки или изменении items

## Архитектурные решения

### 1. Единственный источник истины

Категории берутся из данных продуктов (backend), а не хардкодятся:

```typescript
❌ НЕПРАВИЛЬНО:
const categories = ['Фрукты', 'Овощи', 'Мясо'];

✅ ПРАВИЛЬНО:
const categories = useMemo(() => {
  // Динамически извлекаем из items
}, [items]);
```

**Почему так:**
- Если backend добавит новую категорию - вкладка появится автоматически
- Если удалить все продукты категории - вкладка исчезнет
- Нет рассинхронизации между backend и frontend

### 2. State Management

```typescript
const [activeTab, setActiveTab] = useState<string>('all');
```

**Альтернативы (НЕ используются):**
- ❌ URL Query параметры (`?category=fruits`)
  - Сложнее для простой UI фильтрации
  - Не нужно для SEO (приватная страница)
  
- ❌ Zustand store
  - Излишне для локального UI состояния
  - Не нужно шарить между компонентами

**Текущее решение:**
- ✅ Локальный `useState` - простота и производительность
- Состояние сбрасывается при перезагрузке страницы (это ОК для UX)

### 3. Сортировка категорий

```typescript
.sort((a, b) => a.name.localeCompare(b.name, 'ru'))
```

**Особенности:**
- `localeCompare` - правильная сортировка русских букв (А-Я)
- Аргумент `'ru'` - учитывает специфику русского алфавита
- Стабильная сортировка категорий между рендерами

## Тестирование

### Сценарии для проверки

1. **Базовая навигация:**
   - Клик на "Все" → показываются все продукты
   - Клик на "Фрукты" → только фрукты
   - Badge показывает правильное количество

2. **Empty State:**
   - Выбрать категорию без продуктов
   - Проверить сообщение "В категории X нет продуктов"

3. **Динамическое обновление:**
   - Добавить продукт новой категории → вкладка появляется
   - Удалить последний продукт категории → вкладка исчезает
   - Badge обновляется при добавлении/удалении

4. **Адаптивность:**
   - Проверить на разных размерах экрана
   - Вкладки должны переноситься корректно

5. **Производительность:**
   - Переключение между вкладками должно быть мгновенным
   - Нет лишних ререндеров (проверить React DevTools)

## Будущие улучшения

### Возможные доработки:

1. **Сохранение выбранной вкладки:**
   ```typescript
   // В localStorage или URL query
   useEffect(() => {
     localStorage.setItem('lastActiveTab', activeTab);
   }, [activeTab]);
   ```

2. **Индикаторы статуса на вкладках:**
   ```tsx
   <button>
     🍎 Фрукты
     <span className="badge">5</span>
     {hasExpiringItems && <span className="warning">⚠️</span>}
   </button>
   ```

3. **Поиск внутри категории:**
   ```tsx
   {activeTab !== 'all' && (
     <input placeholder="Поиск в категории..." />
   )}
   ```

4. **Drag-and-drop сортировка:**
   - Пользователь может менять порядок вкладок
   - Сохранение порядка в localStorage

5. **Группировка по статусу:**
   ```tsx
   <Tabs>
     <Tab>Все</Tab>
     <Tab>🟢 В норме</Tab>
     <Tab>🟡 Заканчиваются</Tab>
     <Tab>🔴 Просрочены</Tab>
   </Tabs>
   ```

## Связанные файлы

- `app/[locale]/inventory/page.tsx` - основной компонент с вкладками
- `lib/api/inventory.ts` - типы данных, категории
- `docs/CATEGORIES_ARCHITECTURE.md` - общая архитектура категорий
