# 🎯 Автоматический расчёт срока годности - Changelog

## Дата: 9 февраля 2026

### ✅ Реализовано

#### 1. Backend Integration (уже работает)
- ✅ Backend автоматически рассчитывает `expires_at = received_at + default_shelf_life_days`
- ✅ Молоко: 7 дней → `2026-02-09 + 7d = 2026-02-16`
- ✅ Авокадо: 5 дней → `2026-02-09 + 5d = 2026-02-14`
- ✅ Поддержка ручного override через `expires_at` в POST запросе

#### 2. Frontend - UI/UX улучшения

**A. Модалка добавления продукта** (`components/inventory/add-product-modal.tsx`)
- ✅ Превью срока годности на русском языке
- ✅ Формат: "Срок годности (авто): 2026-02-16"
- ✅ Подсказка: "(получено 2026-02-09 + 7 дней хранения)"

**B. Карточки инвентаря** (`app/[locale]/inventory/page.tsx`)
- ✅ Цветовая индикация дат:
  - 🔴 Красный: просрочен
  - 🟠 Оранжевый: истекает (0-3 дня)
  - ⚫ Серый: норма
- ✅ Динамические метки:
  - "(просрочен)" - если days < 0
  - "(сегодня!)" - если days = 0
  - "(завтра)" - если days = 1
  - "(Xд)" - если days <= 7
- ✅ Кнопка удаления продукта с подтверждением

**C. API Layer** (`lib/api/inventory.ts`)
- ✅ Улучшенные warnings:
  - "Просрочен на X дней"
  - "⚠️ Истекает сегодня!"
  - "Истекает завтра"
  - "Осталось X дня/дней"
- ✅ Дополнительное предупреждение для 4-7 дней

**D. Обработка DELETE запросов** (`lib/api/client.ts`)
- ✅ Обработка HTTP 204 No Content
- ✅ Возвращает `null` для пустых ответов (DELETE)
- ✅ Безопасная типизация: `Promise<T | null>`
- ✅ Проверки на null во всех API функциях:
  - `searchCatalogIngredients` → `res?.ingredients || []`
  - `fetchInventory` → `if (!dtos) return []`
  - `registerUser`, `loginUser`, etc. → `throw if null`

#### 3. Документация
- ✅ Создан `docs/INVENTORY_EXPIRATION.md` с полным описанием:
  - Логика автоматического расчёта
  - Приоритеты статусов
  - Query DTO Pattern
  - POST → GET Pattern
  - Примеры тестирования

### 🎨 UI Changes Summary

#### До:
```
⏳ Годен до: 09.02.2026
```

#### После:
```
⏳ Годен до: 09.02.2026 (сегодня!)     [оранжевый цвет]
⏳ Годен до: 10.02.2026 (завтра)       [оранжевый цвет]
⏳ Годен до: 14.02.2026 (5д)           [оранжевый цвет]
⏳ Годен до: 05.02.2026 (просрочен)    [красный цвет]
⏳ Годен до: 20.02.2026                [серый цвет - норма]
```

### 📊 Статусы и цвета

| Статус | Эмодзи | Цвет | Условие |
|--------|--------|------|---------|
| Просрочен | 🔴 | Красный | `days < 0` |
| Истекает | 🟠 | Оранжевый | `0 <= days <= 3` |
| Мало | 🟡 | Жёлтый | `quantity < 5` |
| В норме | 🟢 | Зелёный | Всё остальное |

### 🔧 Technical Details

#### Type Safety
```typescript
// client.ts
export async function apiFetch<T>(...): Promise<T | null>

// Обработка 204 No Content
if (res.status === 204) {
  return null;
}

// Проверки на null
if (!result) {
  throw new Error('Empty response from server');
}
```

#### Status Calculation
```typescript
function calculateStatus(expiresAt: string | null, quantity: number) {
  if (days < 0) status = 'expired';
  else if (days <= 3) status = 'expiring';
  else if (quantity < 5) status = 'low';
  else status = 'in-stock';
}
```

### 🧪 Testing

```bash
# Тест 1: Автоматический расчёт (Молоко)
curl -X POST /api/inventory/products \
  -d '{"catalog_ingredient_id":"...","received_at":"2026-02-09T10:00:00Z"}'
# Result: expires_at = 2026-02-16T10:00:00Z ✅

# Тест 2: Автоматический расчёт (Авокадо)
curl -X POST /api/inventory/products \
  -d '{"catalog_ingredient_id":"...","received_at":"2026-02-09T10:00:00Z"}'
# Result: expires_at = 2026-02-14T10:00:00Z ✅

# Тест 3: Ручной override
curl -X POST /api/inventory/products \
  -d '{"received_at":"...","expires_at":"2026-02-20T23:59:59Z"}'
# Result: expires_at = 2026-02-20T23:59:59Z ✅

# Тест 4: DELETE продукта
curl -X DELETE /api/inventory/products/{id}
# Result: HTTP 204 No Content ✅
```

### 📁 Modified Files

```
lib/api/client.ts                               [MODIFIED] +7 lines
lib/api/inventory.ts                            [MODIFIED] +15 lines
lib/api/auth.ts                                 [MODIFIED] +30 lines
components/inventory/add-product-modal.tsx      [MODIFIED] +3 lines
app/[locale]/inventory/page.tsx                 [MODIFIED] +25 lines
docs/INVENTORY_EXPIRATION.md                    [CREATED]
```

### 🚀 Next Steps

1. ✅ Протестировать удаление продукта через UI
2. ⏳ Добавить функционал редактирования продукта
3. ⏳ Добавить bulk operations (массовое удаление)
4. ⏳ Добавить фильтры по статусу (показать только истекающие)
5. ⏳ Добавить сортировку по дате истечения

### 📈 Impact

**UX Improvements:**
- 🎨 Визуальная индикация критичности срока годности
- 📅 Быстрое определение продуктов требующих внимания
- 🚀 Автоматический расчёт срока годности (меньше ручной работы)

**Code Quality:**
- ✅ Type-safe обработка nullable responses
- ✅ Корректная обработка HTTP 204 No Content
- ✅ Централизованная логика статусов
- ✅ Query DTO Pattern для избежания N+1 queries

**Performance:**
- ✅ POST → GET pattern (всегда свежие данные)
- ✅ Single source of truth (backend)
- ✅ No redundant calculations on frontend
